import {View, Text, FlatList} from 'react-native';
import React from 'react';
import axios from 'axios';

const ProductList = () => {
  return (
    <View>
      <Text>Hai mahn</Text>
      {/* <FlatList keyExtractor={item => item.id.toString()} horizontal /> */}
    </View>
  );
};

export default ProductList;
